<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mislugares.example.jhon.misluguares" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/TemaMisLugares" >
        <activity
            android:name=".LugarMainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".EdicionLugarActivity"
            android:label="Edición Lugar">

        </activity>
        <!-- Se agrega la actividad para el Acerca De ... -->
        <activity android:name=".AcercaDe"
            android:label="Acerca De ..."
            android:theme="@android:style/Theme.Dialog" >

            </activity>
        

    </application>

</manifest>
